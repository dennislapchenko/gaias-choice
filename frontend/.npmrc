# Supply-chain hardening: never run package lifecycle scripts (postinstall, etc.).
# The chosen deps do not require build scripts, so this is safe and blocks the
# most common npm RCE vector.
ignore-scripts=true
audit=true
fund=false
